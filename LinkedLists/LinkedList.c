#include <stdlib.h>
#include <stdio.h>

struct node {
    //variable to store the value of the data in a particular index of the linked list
    int data;
    struct Node* next;
};


int main(){


    return 0;
}